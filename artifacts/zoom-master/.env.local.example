# Copy to .env.local for local frontend dev (never commit)

# Vercel (Project Settings → Environment Variables):
#   VITE_API_BASE = https://YOUR-SERVICE.onrender.com/api
#
# Local dev: leave unset — Vite proxies /api → http://127.0.0.1:3001
#
# Browser dev (no Telegram Mini App): auto-login as this Telegram ID + admin panel.
# VITE_DEV_TELEGRAM_ID=8144744644
